import { useState } from 'react';
import styled from 'styled-components';

function Listing({token_id}) {
    return <Container>
    </Container>
}

const Container = styled.div`

`

export default Listing;