{
  "private": true,
  "scripts": {
    "install": "lerna bootstrap",
    "build": "lerna run build",
    "test": "lerna run test",
    "lint": "lerna run lint",
    "lint:fix": "lerna run lint:fix",
    "prettier": "prettier --check . && lerna run prettier",
    "prettier:write": "prettier --write . && lerna run prettier:write"
  },
  "devDependencies": {
    "lerna": "^3.22.1",
    "prettier": "^2.5.0"
  }
}
